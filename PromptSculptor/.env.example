# Server Configuration
NODE_ENV=development
PORT=5000

# Database
DATABASE_URL=your-database-connection-string-here

# API Keys (CRITICAL: Keep these secret, rotate regularly)
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Security Keys (CRITICAL: Generate strong random values)
# ADMIN_API_KEY=your-secure-api-key-here (DEPRECATED - replaced by Google OAuth)
SESSION_SECRET=your-session-secret-here
ENCRYPTION_KEY=your-64-hex-character-encryption-key-here

# Admin OAuth Configuration (Google)
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
ADMIN_ALLOWED_EMAILS=admin@yourdomain.com,backup-admin@yourdomain.com

# Client Configuration (for development/testing)
VITE_API_KEY=dev-api-key-123

# Security Configuration (Production)
CORS_ORIGINS=http://localhost:3000,http://localhost:5001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AI_RATE_LIMIT_PER_MINUTE=10

# Email Configuration (Resend)
RESEND_API_KEY=your-resend-api-key-here
EMAIL_FROM=noreply@yourdomain.com
APP_URL=http://localhost:5001

# PRODUCTION SECURITY NOTES:
# - NEVER commit .env files to version control
# - Use different secrets for development/staging/production
# - Generate secure API keys with: openssl rand -hex 32
# - Store production secrets in secure vault (AWS Secrets Manager, etc.)
# - Rotate all secrets before production deployment
#
# GOOGLE OAUTH SETUP:
# 1. Create OAuth 2.0 credentials at: https://console.developers.google.com/
# 2. Set authorized redirect URI: http://localhost:5001/api/admin/auth/google/callback
# 3. For production, update APP_URL and add production callback URI
# 4. Add admin email addresses to ADMIN_ALLOWED_EMAILS (comma-separated)